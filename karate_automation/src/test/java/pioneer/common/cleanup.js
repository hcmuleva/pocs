function (setup) {
    karate.log("Clean up started...");
    /**
        This function is to call required cleanup feature files.
        e.g. Clean token, delete user role, group etc...
            var token = karate.call('classpath:pioneer/reusable/_oauth2_token.feature');
    */

}